// 배열 메소드
// pop push unshift shift splice
// concat join split slice indexOf


const text = '자바스크립트, 프론트엔드, HTML, CSS';
const splitArray = text.split(', ');
console.log(splitArray);

const text2 = ['자바', '자바스크립트', '스프링']
const joinArray = text2.join(', ');
console.log(joinArray);

